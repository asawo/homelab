dir: local
	path /var/lib/vz
	content iso,vztmpl,import,backup

lvmthin: local-lvm
	thinpool data
	vgname pve
	content images,rootdir

dir: backups
	path /mnt/storage/backups/vzdump
	content backup
	is_mountpoint /mnt/storage
	prune-backups keep-all=1


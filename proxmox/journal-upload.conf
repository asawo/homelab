# systemd-journal-upload client config for the host itself (pve.lan).
# Same content pushed to Copyparty/FileBrowser/LeafWiki as
# monitoring/journal-upload.conf -- tracked separately here since it's part
# of the host's own config set (pushed like network.interfaces, not secret).
# Install: apt install systemd-journal-remote (ships both sender and receiver)
# Place at: /etc/systemd/journal-upload.conf
# Enable: systemctl enable --now systemd-journal-upload.service

[Upload]
URL=http://192.168.11.208:19532

# check-storage.sh configuration
# Copy to check-storage.env, fill in values, and place at /usr/local/etc/check-storage.env on the host
# (or `just push-pve` will push it there if it exists locally)
NTFY_TOPIC=

# systemd-journal-upload client config, fanned out via `just push-monitoring`
# to Copyparty (102), FileBrowser (105), and LeafWiki (106) -- the 3 native-
# systemd, no-Docker services in scope. Same content pushed to the host
# separately as proxmox/journal-upload.conf.
# Install: apt install systemd-journal-remote (ships both sender and receiver)
# Place at: /etc/systemd/journal-upload.conf
# Enable: systemctl enable --now systemd-journal-upload.service

[Upload]
URL=http://192.168.11.208:19532
